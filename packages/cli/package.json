{
  "name": "@escad/cli",
  "version": "0.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "force-build": "npm run clean; tsc",
    "hash": "find bin/ -type f -print0 | sort -z | xargs -0 openssl dgst -sha256 | openssl dgst -sha256 > .hash",
    "changed": "touch .hash; mv .hash old.hash; npm run hash; cmp old.hash .hash >/dev/null 2>&1",
    "build": "npm run changed && echo 'Skipping build' || npm run force-build; rm old.hash",
    "prepack": "npm run force-build"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "commander": "^5.1.0",
    "@escad/server": "^0.0.0"
  },
  "devDependencies": {
    "@types/commander": "^2.12.2",
    "typescript": "^4.1.0-dev.20201015"
  },
  "bin": {
    "escad": "dist/bin/escad.js",
    "escad-serve": "dist/bin/escad-serve.js"
  },
  "publishConfig": {
    "access": "public"
  }
}
