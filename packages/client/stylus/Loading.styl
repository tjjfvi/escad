
.Loading
  display inline-flex
  align-items center
  justify-content center
  --size 45px
  width var(--size)
  height var(--size)
  position relative

  &:before
  &:after
    content ""
    display inline-block
    border calc(var(--size) * .0667) solid transparent
    border-top-color currentColor
    border-bottom-color currentColor
    border-radius 100%
    animation-name spin
    animation-iteration-count infinite
    animation-timing-function linear
    position absolute

  &:before
    animation-duration 2s
    width 66.7%
    height 66.7%

  &:after
    animation-duration 1.333s
    animation-direction reverse
    width 44.4%
    height 44.4%
