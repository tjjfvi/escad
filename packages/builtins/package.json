{
  "name": "@escad/builtins",
  "version": "0.0.0",
  "description": "",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "clean": "rm -rf dist",
    "build": "npm run clean; tsc",
    "hash": "find src/ -type f -print0 | sort -z | xargs -0 openssl dgst -sha256 | openssl dgst -sha256 > .hash",
    "changed": "touch .hash; mv .hash old.hash; npm run hash; cmp old.hash .hash >/dev/null 2>&1",
    "quick-build": "npm run changed && echo 'Skipping build' || npm run build; rm old.hash",
    "prepack": "npm run force-build"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^4.1.0-dev.20201015"
  },
  "dependencies": {
    "@escad/mesh": "^0.0.0",
    "@escad/csg": "^0.0.0",
    "@escad/solids": "^0.0.0",
    "@escad/transform": "^0.0.0"
  },
  "publishConfig": {
    "access": "public"
  }
}
