{
  "name": "escad",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "prebootstrap": "cp package-lock-lock.json package-lock.json",
    "bootstrap": "lerna exec 'rm -rf node_modules package-lock.json'; lerna bootstrap --hoist; patch-package",
    "postbootstrap": "cp package-lock.json package-lock-lock.json",
    "test": "npm run quick-build && jest -u",
    "quick-build": "tsc -b packages",
    "build-playground": "mkdir -p packages/playground/static/packages; lerna run build-for-playground --parallel; lerna run build-playground",
    "build": "npm run bootstrap; npm run index; npm run quick-build",
    "force-build": "lerna run clean; npm run build",
    "index": "create-index -X ts -x ts -x tsx -Sui packages/ -c {}",
    "lint": "eslint '**/*.{ts,tsx}' --quiet --fix",
    "open-coverage": "open coverage/lcov-report/index.html",
    "escad": "npm run quick-build && node packages/cli/dist/bin/escad.js",
    "playground": "node packages/playground/dist/server/devServer.js",
    "open-errors": "npm run quick-build | grep packages/ | sed -e 's/(.*//' | sort | uniq | xargs code",
    "postinstall": "npm run bootstrap",
    "build-playground-prod": "npm run force-build && npm run build-playground && node packages/playground/dist/server/buildProduction.js",
    "static": "node -e 'let e=require(`express`);e().use(e.static(process.argv[1])).listen(process.argv[2]||8080)' --"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "@typescript-eslint/eslint-plugin": "^4.14.1",
    "@typescript-eslint/parser": "^4.14.1",
    "create-index-better": "^1.4.4",
    "eslint": "^7.18.0",
    "eslint-plugin-react": "^7.22.0",
    "jest": "^26.6.3",
    "lerna": "^3.22.1",
    "patch-package": "^6.2.2",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.1.1",
    "typescript": "^4.1.3"
  },
  "private": true
}